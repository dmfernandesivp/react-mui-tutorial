import { Box } from "@mui/material";
import React from "react";

const Settings: React.FC = () => {
  return (
    <Box sx={{ mt: 64 }}>
      <p>Settings</p>
    </Box>
  );
};

export default Settings;
